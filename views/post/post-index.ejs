<% include ../partials/header %>

<a href= '/post/new'>Add post</a>
<% for (let posts of data) { %>
    <div class= 'post'>
    <p><%= posts.description %></p>
    <img src=<%= posts.image %> size=50% />

    <a href='/post/<%= posts.id%>'>Edit Post</a>

    </div>
  <% } %>

<% include ../partials/footer %>
